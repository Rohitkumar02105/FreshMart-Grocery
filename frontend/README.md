# FreshMart Frontend

React frontend powered by Vite.

```bash
npm install
npm run dev
```

Set `VITE_API_URL` only when the API is hosted on a different origin. During local development, `/api` requests are proxied to `http://localhost:5000`.
